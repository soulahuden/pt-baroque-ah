services:
  app:
    build: .
    ports:
      - "80:3000"
    environment:
      - PORT=3000
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - DB_NAME=${DB_NAME}
      - SESSION_SECRET=${SESSION_SECRET}
      - WAF_MODE=${WAF_MODE:-detect}
      - MONITOR_TOKEN=${MONITOR_TOKEN}
      - EVE_JSON_PATH=/var/log/suricata/eve.json
      - COWRIE_JSON_PATH=/var/log/cowrie/cowrie.json
    volumes:
      - suricata-logs:/var/log/suricata:ro
      - cowrie-logs:/var/log/cowrie:ro
    networks:
      - frontend
      - backend
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASS}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASS}
    volumes:
      - mysql-data:/var/lib/mysql
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - backend
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      retries: 10
    restart: unless-stopped

  suricata:
    image: jasonish/suricata:latest
    network_mode: host
    cap_add:
      - NET_ADMIN
      - NET_RAW
      - SYS_NICE
    volumes:
      - suricata-logs:/var/log/suricata
      - ./suricata/suricata.yaml:/etc/suricata/suricata.yaml
      - ./suricata/custom.rules:/var/lib/suricata/rules/custom.rules
    command: -i ${SURICATA_INTERFACE:-eth0}
    restart: unless-stopped

  cowrie:
    image: cowrie/cowrie:latest
    ports:
      - "22:2222"
      - "23:2223"
    volumes:
      - cowrie-logs:/cowrie/var/log/cowrie
    environment:
      - COWRIE_HOSTNAME=webserver-prod-01
    restart: unless-stopped


networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge
    internal: true

volumes:
  suricata-logs:
  cowrie-logs:
  mysql-data:
